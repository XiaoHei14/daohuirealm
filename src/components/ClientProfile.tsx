'use client';
import { useEffect } from "react"
import gsap from "gsap";

export default function ClientProfile() {
  return (
    <section className="relative w-full min-h-screen flex justify-center items-center bg-violet-600 overflow-hidden p-4"></section>
  );
}